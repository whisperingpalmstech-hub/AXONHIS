import requests, json\n\ntoken = requests.post('http://localhost:8000/api/v1/auth/login', data={'username':'admin','password':'password'}).json()['access_token']\nheaders = {'Authorization': f'Bearer {token}'}\n\nfor url in ['http://localhost:8000/api/v1/patients', 'http://localhost:8000/api/v1/encounters', 'http://localhost:8000/api/v1/billing/insurance/providers']:\n  try:\n    r = requests.get(url, headers=headers)\n    print(f'\n{url} -> {r.status_code}')\n    if r.status_code == 200:\n      data = r.json()\n      print(json.dumps(data[:1] if isinstance(data, list) else data, indent=2))\n  except Exception as e:\n    print(e)\n
